import os

from dotenv import load_dotenv
load_dotenv()

# TODO:
# set up connection to Tableau Cloud service to:
# - send/store data from our supabase PSQL db to Tableau Cloud
# that's about it... if there are more implementations that we might need add them here